// Easy

const car = {
brand: "Volvo",
model: "XC60", // forgot comma
year: 2020,
color: "pink" // needed color
};
//console.log(car.color);

//

const point = { x: 10, y: 20 }; // wrong symbol(] instead of })
//console.log(point.x);

//

const user = {
name: "Mila",
email: "mila@mail.com" // to many L
};

//console.log(user.email);

//

const profile = { contact: { email: "a@b.com" } };
console.log(profile.contact.email); // need to choose the object with in the object to access the email